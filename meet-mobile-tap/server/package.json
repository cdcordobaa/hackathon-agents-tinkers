{
  "name": "xentinela-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm --prefix ../agent run dev",
    "test": "node --import tsx --test src/*.test.ts",
    "typecheck": "tsc --noEmit",
    "verify-ingest": "node --import tsx scripts/prove-segment-ingest.ts",
    "verify-join": "node --import tsx scripts/prove-join-fix.ts"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/ws": "^8.5.13",
    "tsx": "^4.20.5",
    "typescript": "^5.7.0"
  }
}
