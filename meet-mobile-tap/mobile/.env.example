# The only mobile configuration is the public join gateway. LiveKit provider
# credentials stay on the server and must never use an EXPO_PUBLIC_ prefix.
EXPO_PUBLIC_GATEWAY_URL=http://localhost:8787

# A physical phone cannot reach your computer through localhost. Replace it
# with the computer's LAN address, for example http://192.168.1.20:8787.
